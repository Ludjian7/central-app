# This application uses mock data instead of a real database connection
# The following environment variables are still used for configuration

# Session Secret
SESSION_SECRET=your_session_secret_here

# Node Environment
NODE_ENV=development

# Server Port
PORT=5001

# Demo Mode (always true) 